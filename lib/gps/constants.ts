/** Centro por defecto del mapa (Córdoba, AR). */
export const DEFAULT_MAP_CENTER = {
  latitude: -31.4167,
  longitude: -64.1833,
} as const

export const DEFAULT_MAP_ZOOM = 13
export const SELECTED_LOCATION_MAP_ZOOM = 16
