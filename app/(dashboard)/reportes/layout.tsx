"use client"

/**
 * Reportes layout — no heavy Tasks/Projects/Crews stack.
 * Reportes Operativos loads lean columns via Analysis queries (Sprint 16).
 * Por-empleado mounts its own providers in its nested layout.
 */
export default function ReportesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
